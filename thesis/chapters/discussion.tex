% Source material: docs/seminar/scientific-narrative.md — Validation Philosophy, Definition of Success

\chapter{Discussion}
\label{ch:discussion}

% ============================================================
\section{Interpretation of Spatial Overlap}
\label{sec:discussion-dice}
% ============================================================

% TODO: Dice of ~0.28 must be contextualized against the ~15× streamline count difference
%       (Section~\ref{subsec:tractoinferno-streamlines}).
%       When |candidate| ≪ |reference|, Dice is mathematically bounded below 2|candidate|/|reference|;
%       given a ~15× ratio, a Dice ceiling of ~0.13 would apply to purely random candidate streamlines,
%       so 0.28 reflects genuine spatial specificity, not random overlap.
%       Precision of 0.854 directly shows that the vast majority of csttool streamlines fall
%       within the reference bundle — the bundle boundaries are correct even if density is lower.
%       Cite the TractoInferno paper's own note that Dice is sensitive to bundle density differences.
%       Frame as a deliberate precision-focused design choice appropriate for clinical CST assessment:
%       in an ALS biomarker context, false positives (non-CST streamlines contaminating tract metrics)
%       are more harmful than false negatives (missing low-confidence streamlines).

% ============================================================
\section{Hemispheric Asymmetry}
\label{sec:discussion-asymmetry}
% ============================================================

% TODO: Right CST yielded consistently higher Dice than left (0.299 vs 0.268, Section~\ref{subsec:tractoinferno-dice}).
%       Discuss in the context of known right-hemisphere CST volume asymmetry in healthy adults.
%       Note that this is a population-level observation across 167 subjects; the direction of
%       asymmetry is consistent with the literature.
%       Keep brief — 1–2 paragraphs.

% ============================================================
\section{Determinism as a Methodological Strength}
\label{sec:discussion-determinism}
% ============================================================

% TODO: Bitwise reproducibility (Section~\ref{sec:results-determinism}) is a critical property
%       for longitudinal ALS monitoring: if a patient is scanned twice, any change in tract metrics
%       must reflect true biological change, not pipeline-level variability.
%       Contrast with probabilistic tractography where repeated runs on identical data produce
%       different streamline sets and therefore different metrics.
%       Note that determinism is guaranteed given fixed preprocessed inputs — preprocessing itself
%       (e.g., FSL eddy correction) is a one-time step whose output is saved and reused.
%       Seed 42 and single-threaded execution (OMP_NUM_THREADS=1) are the two technical
%       mechanisms that enforce this guarantee.

% ============================================================
\section{Limitations}
\label{sec:discussion-limitations}
% ============================================================

% TODO: Address the following limitations honestly:
%
%   (a) Healthy adult cohort only — TractoInferno contains no ALS patients or other pathological
%       cases. The ability of csttool to correctly delineate a partially degenerated CST remains
%       untested. Pathological ground truth is needed before clinical deployment.
%
%   (b) Personal DWI acquisition and eddy-corrected anonymous data pending (expected post April 22).
%       Results with distortion correction (FSL topup+eddy) are explicitly deferred; the current
%       validation represents uncorrected data only.
%
%   (c) Single-peak deterministic tracking misses fiber crossings — in regions where the CST
%       intersects with other white matter tracts (e.g., superior corona radiata), streamlines
%       may terminate prematurely or deviate, contributing to the lower recall observed.

% ============================================================
\section{Future Work}
\label{sec:discussion-future}
% ============================================================

% TODO: Outline the following next steps:
%
%   1. Personal DWI acquisition (April 24): process with and without FSL topup+eddy correction;
%      compare FA/MD and streamline counts between corrected and uncorrected pipelines.
%
%   2. Eddy current correction evaluation: integrate NiFreeze as a fallback correction backend
%      (--correction-backend {none,dipy,nifreeze}) for datasets without an AP/PA pair.
%
%   3. Extension to pathological cohort: apply csttool to ALS patient DWI data to validate
%      sensitivity to CST degeneration and test clinical utility.
%
%   4. Batch processing at scale: BIDS-compatible batch mode for multi-subject studies.
